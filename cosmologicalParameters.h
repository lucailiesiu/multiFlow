class cosmoParam{
  private:
    
  public:
    double r, n, run, f_nl;
    cosmoParam(lambda);
    double calculateN(lambda);
    double calculateR(lambda);
    double calculatefNL(lambda);
    
}
    
    
